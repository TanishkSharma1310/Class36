var canvas,bgimg;
var playerCount,form,player,game,database;
var gameState = 0;

function setup(){

  canvas = createCanvas(500,500);
database = firebase.database();

}

function draw(){
  background("white");
  
    
}
